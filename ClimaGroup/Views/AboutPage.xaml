<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ClimaGroup.Views"
             xmlns:viewmodels="clr-namespace:ClimaGroup.ViewModels"
             x:Class="ClimaGroup.Views.AboutPage">

    <ContentPage.BindingContext>
        <viewmodels:AboutViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="20">
            <Label Text="Nuestro Equipo" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />

            <CollectionView ItemsSource="{Binding Miembros}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <views:MiembroView />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
